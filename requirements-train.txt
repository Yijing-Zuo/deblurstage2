# Separate deblur-paddle-train environment: upstream training uses headless cv2.
# Keep deblur-paddle/PaddleX and its opencv-contrib-python unchanged for inference.
# Install the official GPU wheel first:
# python -m pip install paddlepaddle-gpu==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
numpy==1.26.4
Pillow==11.3.0
PyYAML==6.0.2
# Dependencies imported by the pinned upstream PaddleOCR training package.
lmdb>=1.6,<2
rapidfuzz>=3,<4
shapely>=2,<3
pyclipper>=1.3,<2
scikit-image>=0.22,<0.26
scipy>=1.11,<1.15
Cython>=3,<4
tqdm>=4.66,<5
requests>=2.31,<3
packaging>=24,<26
albumentations==2.0.8
albucore==0.0.24
# A single cv2 distribution satisfies the upstream trainer and Albumentations.
opencv-python-headless==4.10.0.84
